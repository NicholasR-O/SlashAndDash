Particle prefabs for the car are created here automatically by `RuntimeParticleAssetBootstrap` when Unity reloads scripts.

Edit the generated prefabs and `ParticleBillboard.mat` here to tune particles. Put swappable particle textures in `Assets/Particles/textures` and assign them from the Player inspector.
